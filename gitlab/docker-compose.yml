services:
  gitlab-runner:
    image: gitlab/gitlab-runner:latest
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config:/etc/gitlab-runner:Z # Save the configuration file
    restart: unless-stopped
    command: ["gitlab-runner", "run"]
